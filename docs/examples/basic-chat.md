---
sidebar_position: 1
title: Basic Chat App
---

# Example: Basic chat app

{/* CODEX: Write a complete, runnable SwiftUI example for a basic LLM chat app.

  Structure:
  ## What this example builds
  A minimal iOS/macOS chat app that loads a local model and streams multi-turn conversation.

  ## Prerequisites
  - Edge Kit installed via SPM
  - A local model directory (e.g., Qwen3.5-0.8B)

  ## Complete code
  Full SwiftUI app (~60-80 lines):
  - ContentView with text input + send button + scrollable output
  - ViewModel using LLMEngine: load model, generate streaming, multi-turn history
  - Show loading state, streaming text, error handling

  Use real API: LLMEngine(), loadLocal(directory:), generate(messages:), ChatMessage, clearPromptCache()

  ## Key concepts
  Brief list: streaming async sequence, multi-turn with prompt cache, @MainActor

  ## Next steps
  Links to: Vision chat, API reference

  DO NOT EXPOSE any internal algorithms.
*/}
